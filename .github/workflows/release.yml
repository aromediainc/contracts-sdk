name: Release

on:
  push:
    branches: [main]

# Only one release job in flight at a time — concurrent merges shouldn't
# race on creating the Version PR or publishing.
concurrency:
  group: release-${{ github.workflow }}
  cancel-in-progress: false

# Required permissions for the changesets action to open PRs, push tags,
# and (when `provenance` is enabled in package.json) publish to npm with
# attestations.
permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release:
    name: Version + Publish
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Changesets needs full history to compute changelogs and tags.
          fetch-depth: 0
          # RELEASE_PAT (fine-grained PAT with contents + pull-requests
          # write) is required because the enterprise policy forbids the
          # built-in GITHUB_TOKEN from creating PRs. It also lets the
          # Version PR's commits trigger CI, which GITHUB_TOKEN never does.
          token: ${{ secrets.RELEASE_PAT || github.token }}
          persist-credentials: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          # registry-url is required so `npm publish` knows where to push
          # and how to interpret NODE_AUTH_TOKEN.
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Create Release PR or Publish
        id: changesets
        uses: changesets/action@v1
        with:
          # `version` consumes pending changesets and updates package.json
          # + CHANGELOG.md in a PR titled "Version Packages". `publish`
          # runs after the Version PR merges (when there are no pending
          # changesets) and pushes to npm.
          version: npm run version
          publish: npm run release
          commit: "chore: version packages"
          title: "chore: version packages"
        env:
          # Must be the PAT, not the built-in token — see checkout step.
          GITHUB_TOKEN: ${{ secrets.RELEASE_PAT || github.token }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
