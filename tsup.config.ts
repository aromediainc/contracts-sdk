// tsup options are passed via the `build` script in package.json so we can
// stay on a config-free flow in this sandboxed build environment. This file
// is intentionally minimal; do not import anything here.
export default {};
